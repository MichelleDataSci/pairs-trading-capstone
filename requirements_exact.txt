# Exact dependency versions used to produce all project outputs.
# Generated on 2026-08-31 from the environment that ran the full pipeline.
#
# To reproduce the exact environment:
#   pip install -r requirements_exact.txt
#
# requirements.txt uses >= floor pins for flexibility.
# This file uses == pins so results are exactly reproducible.

yfinance==1.3.0
pandas==2.3.2
numpy==2.3.3
statsmodels==0.14.6
scipy==1.16.3
matplotlib==3.10.8
seaborn==0.13.2
plotly==6.7.0
streamlit==1.57.0
nltk==3.9.4
requests==2.32.4
beautifulsoup4==4.13.4
